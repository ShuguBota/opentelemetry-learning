# OpenTelemetry Learning

Project done to understand better how Open Telemetry works.

Runs based on the DockerFile.

```bash
docker build -t opentelemetry-example .
docker run -p 8080:8080 opentelemetry-example
```
